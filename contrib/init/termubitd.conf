description "Termubit Core Daemon"

start on runlevel [2345]
stop on starting rc RUNLEVEL=[016]

env TRBCOIND_BIN="/usr/bin/termubitd"
env TRBCOIND_USER="termubit"
env TRBCOIND_GROUP="termubit"
env TRBCOIND_PIDDIR="/var/run/termubitd"
# upstart can't handle variables constructed with other variables
env TRBCOIND_PIDFILE="/var/run/termubitd/termubitd.pid"
env TRBCOIND_CONFIGFILE="/etc/termubit/termubit.conf"
env TRBCOIND_DATADIR="/var/lib/termubitd"

expect fork

respawn
respawn limit 5 120
kill timeout 60

pre-start script
    # this will catch non-existent config files
    # termubitd will check and exit with this very warning, but it can do so
    # long after forking, leaving upstart to think everything started fine.
    # since this is a commonly encountered case on install, just check and
    # warn here.
    if ! grep -qs '^rpcpassword=' "$TRBCOIND_CONFIGFILE" ; then
        echo "ERROR: You must set a secure rpcpassword to run termubitd."
        echo "The setting must appear in $TRBCOIND_CONFIGFILE"
        echo
        echo "This password is security critical to securing wallets "
        echo "and must not be the same as the rpcuser setting."
        echo "You can generate a suitable random password using the following"
        echo "command from the shell:"
        echo
        echo "bash -c 'tr -dc a-zA-Z0-9 < /dev/urandom | head -c32 && echo'"
        echo
        echo "It is also recommended that you also set alertnotify so you are "
        echo "notified of problems:"
        echo
        echo "ie: alertnotify=echo %%s | mail -s \"Termubit Alert\"" \
            "admin@foo.com"
        echo
        exit 1
    fi

    mkdir -p "$TRBCOIND_PIDDIR"
    chmod 0755 "$TRBCOIND_PIDDIR"
    chown $TRBCOIND_USER:$TRBCOIND_GROUP "$TRBCOIND_PIDDIR"
    chown $TRBCOIND_USER:$TRBCOIND_GROUP "$TRBCOIND_CONFIGFILE"
    chmod 0660 "$TRBCOIND_CONFIGFILE"
end script

exec start-stop-daemon \
    --start \
    --pidfile "$TRBCOIND_PIDFILE" \
    --chuid $TRBCOIND_USER:$TRBCOIND_GROUP \
    --exec "$TRBCOIND_BIN" \
    -- \
    -pid="$TRBCOIND_PIDFILE" \
    -conf="$TRBCOIND_CONFIGFILE" \
    -datadir="$TRBCOIND_DATADIR" \
    -disablewallet \
    -daemon

